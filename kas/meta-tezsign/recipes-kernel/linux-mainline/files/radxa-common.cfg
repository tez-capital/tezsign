# Rockchip platform — SoC and family-wide drivers.
# Enables ARCH_ROCKCHIP and the RK35xx peripherals needed to boot.
CONFIG_ARCH_ROCKCHIP=y
CONFIG_ROCKCHIP_PM_DOMAINS=y
CONFIG_COMMON_CLK_ROCKCHIP=y
CONFIG_IOMMU_DMA=y
CONFIG_ROCKCHIP_IOMMU=y

# Use lazy IOMMU TLB invalidation — strict mode flushes on every
# DMA map/unmap which adds measurable overhead to MMC and USB I/O.
CONFIG_ROCKCHIP_THERMAL=y
CONFIG_PINCTRL_ROCKCHIP=y
CONFIG_GPIO_ROCKCHIP=y
CONFIG_CPUFREQ_DT=y
CONFIG_ARM_ROCKCHIP_CPUFREQ=y

# EXTCON needed for PHY_ROCKCHIP_INNO_USB2 (depends on EXTCON in 6.18).
CONFIG_EXTCON=y

# USB: DWC3 dual-role (respects per-port dr_mode from device tree).
# XHCI is required — DWC3 DUAL_ROLE creates an xhci-hcd platform device
# for the host path. Without it, VBUS-triggered role transitions fail and
# leave the controller in a half-initialized state.
CONFIG_USB_DWC3=y
CONFIG_USB_DWC3_HOST=n
CONFIG_USB_DWC3_DUAL_ROLE=y
CONFIG_USB_DWC3_GADGET=n
CONFIG_USB_XHCI_HCD=y
CONFIG_USB_XHCI_PLATFORM=y
CONFIG_USB_ROLE_SWITCH=n

# USB PHYs — POWER_SUPPLY is required by Inno USB2 PHY for VBUS detection.
CONFIG_POWER_SUPPLY=y
CONFIG_PHY_ROCKCHIP_INNO_USB2=y
CONFIG_PHY_ROCKCHIP_NANENG_COMBO_PHY=y

# Serial: Synopsys DesignWare
CONFIG_SERIAL_8250_DW=y

# PWM
CONFIG_PWM_ROCKCHIP=y

# Crypto / HW RNG
CONFIG_CRYPTO_DEV_ROCKCHIP=y
CONFIG_HW_RANDOM_ROCKCHIP=y

# MMC infrastructure.
# RK3568/RK3566 eMMC uses the DWCMSHC SDHCI variant.
# SD card uses Synopsys DesignWare Mobile Storage (DW-MMC).
CONFIG_MMC_DW=y
CONFIG_MMC_DW_PLTFM=y
CONFIG_MMC_DW_ROCKCHIP=y
# CONFIG_CMA_SIZE_MBYTES=128

# Power and Bus
CONFIG_I2C_RK3X=y
CONFIG_MFD_RK8XX_I2C=y
CONFIG_REGULATOR_RK808=y
CONFIG_REGULATOR_FAN53555=y
CONFIG_ROCKCHIP_IODOMAIN=y

# NVMEM: OTP stores cpu-leakage / speed-bin data used by cpufreq-dt.
CONFIG_NVMEM=y
CONFIG_NVMEM_ROCKCHIP_OTP=y

# DRAM/bus frequency scaling
CONFIG_PM_DEVFREQ=y
CONFIG_PM_DEVFREQ_EVENT=y
CONFIG_DEVFREQ_EVENT_ROCKCHIP_DFI=y
CONFIG_DEVFREQ_THERMAL=y
CONFIG_MMC_HSQ=y