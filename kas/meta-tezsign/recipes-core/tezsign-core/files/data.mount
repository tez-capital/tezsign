[Unit]
Description=Mount Tezsign data partition
DefaultDependencies=no
After=local-fs-pre.target

[Mount]
What=/dev/disk/by-label/data
Where=/data
Type=ext4
Options=rw,noatime,nodiratime,data=journal,nobarrier,commit=60,nofail

[Install]
WantedBy=local-fs.target
