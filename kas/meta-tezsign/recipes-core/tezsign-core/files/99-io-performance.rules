# Force 'none' scheduler for the SD card to reduce kernel processing time
# Only match whole-disk devices (mmcblk0, mmcblk1, ...) — partitions don't have their own scheduler
ACTION=="add|change", KERNEL=="mmcblk[0-9]", ATTR{queue/scheduler}="none"