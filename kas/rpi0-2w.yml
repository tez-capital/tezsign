header:
  version: 14
  includes:
    - base.yml

machine: raspberrypi0-2w-tezsign

local_conf_header:
  release: |
    TEZSIGN_RELEASE_NAME = "rpi0-2w"

  main: |
    RPI_EXTRA_CONFIG:append = "\ndtoverlay=dwc2,dr_mode=peripheral"
    CMDLINE:append = " modprobe.blacklist=brcmfmac,brcmutil,hci_uart,btbcm,bluetooth"